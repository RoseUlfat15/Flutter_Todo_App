class Todo {
  String uid;
  String title;
//  String description;
  bool isComplete;

  Todo({
    required this.uid,
    required this.title,
    // required this.description,
    required this.isComplete,
  });
}
